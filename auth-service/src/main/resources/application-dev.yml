spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  
logging:
  level:
    com.shedlr.authservice: DEBUG
    org.springframework.security: DEBUG
    org.springframework.jdbc: DEBUG
