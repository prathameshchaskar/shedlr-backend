package com.shedlr.authservice.identity.enumtype;

/**
 * ExternalProvider lists supported OAuth2/OpenID Connect identity providers.
 */
public enum ExternalProvider {
    /** Google Identity Platform. */
    GOOGLE
}
